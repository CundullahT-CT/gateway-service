spring:
  #  profiles:
  #    active: dev
  cloud:
    config:
      name: gateway-service
      uri: ${CONFIG_SERVER}
      fail-fast: true
      retry:
        max-attempts: 200
        max-interval: 10000
